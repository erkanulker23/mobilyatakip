# Kurulum: İlk sunucu kurulumunda .env yoksa uygulama .env.example'dan kopyalayıp INSTALL_MODE=1 ekler
# ve kurulum sihirbazını açar. Kurulum tamamlandıktan sonra INSTALL_MODE=0 olur; uygulamayı yeniden başlatın.
# INSTALL_MODE=0

# ========== Database (MySQL) ==========
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=
DB_DATABASE=mobilyatakip

# ========== JWT (Canlıda mutlaka değiştir) ==========
# Canlı: En az 32 karakter rastgele string; aksi halde token'lar tahmin edilebilir.
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=1d

# ========== Mail (bildirim / teklif gönderimi, opsiyonel) ==========
MAIL_HOST=
MAIL_PORT=587
MAIL_USER=
MAIL_PASSWORD=
MAIL_FROM=Mobilya Takip <noreply@example.com>

# ========== Application ==========
NODE_ENV=production
PORT=3001
APP_URL=http://localhost:3001
FRONTEND_URL=http://localhost:3001
# CORS_ORIGINS=https://app.example.com,https://www.example.com

# ========== Canlı (Production) ==========
# - NODE_ENV=production (synchronize otomatik false olur; şema migration ile yönetin)
# - JWT_SECRET: benzersiz, güçlü; DB_*: canlı sunucu bilgileri
# - APP_URL / FRONTEND_URL: canlı domain (HTTPS önerilir)
# Build: npm run build:all
# Çalıştırma: node dist/main.js veya pm2 start ecosystem.config.cjs
# Detay: DEPLOYMENT.md
