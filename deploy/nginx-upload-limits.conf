# Laravel Forge / Nginx — site yapılandırmasına ekleyin (server bloğu içinde).
# Forge: Site → Meta → Files → Edit Nginx Configuration
# client_max_body_size satırını server { } bloğuna ekleyin:
#
#     client_max_body_size 20M;

client_max_body_size 20M;
